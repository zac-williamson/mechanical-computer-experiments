# Repository design entry points

The only current multiplexer is `multiplexer/`. Read its README.md and AGENTS.md before editing. Do not restore prior variants or create parallel multiplexer design folders unless explicitly requested. Historical commits are not current design alternatives.

Other modules are separate workstreams. Preserve their uncommitted changes and do not stage them in a multiplexer commit.
