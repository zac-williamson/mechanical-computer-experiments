See ../README.md for rebuild and assembly instructions. The shared actuator source is ../../multiplexer; no obsolete input meshes are embedded here. Cam-test additionally depends on ../../register. LDraw geometry is read from the local installation; set LDRAW_DIR if necessary.
